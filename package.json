{
  "name": "peek-api",
  "version": "0.2.0",
  "description": "Discover and call internal APIs from any website. Captures XHR/fetch calls, extracts auth headers, outputs structured endpoint catalogs, and makes direct HTTP calls with session cookies.",
  "type": "module",
  "bin": {
    "peek-api": "./bin/cli.js"
  },
  "scripts": {
    "start": "node bin/cli.js"
  },
  "keywords": [
    "api",
    "capture",
    "discover",
    "network",
    "xhr",
    "fetch",
    "browser",
    "playwright",
    "cdp",
    "reverse-engineer",
    "api-discovery",
    "session-cookie",
    "direct-http"
  ],
  "author": "Alex Hillman",
  "license": "MIT",
  "dependencies": {
    "playwright": "^1.50.0",
    "playwright-extra": "^4.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
